pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

rootProject.name = "Tapptitude Template"

include(":app")
include(":network")
include(":config")
include(":core")
